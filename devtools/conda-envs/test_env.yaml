name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - plotly
  - qcelemental

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

